#include <stdio.h>
#include <string.h>
void classifyCharacter(char ch) {
    if (isupper(ch)) {
        printf("Upper\n");
    } else if (islower(ch)) {
        printf("Lower\n");
    } else if (isdigit(ch)) {
        printf("Number\n");
    } else {
        printf("Symbol\n");
    }
}

int main() {
    char ch;
    scanf("%c", &ch);
    classifyCharacter(ch);
    return 0;
}
